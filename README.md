# QUOTEX Admin & Frontend System

## 🛠 Overview

This repository contains:

✔ Backend control (control.json)  
✔ Admin panel (index.html + admin.js)  
✔ Frontend userscript (script.js)

## 📌 Usage

1. Clone or Fork this repo  
2. Edit `control.json` to block/unblock users, assign passwords  
3. Commit & Push to GitHub  
4. Frontend userscript automatically fetches latest `control.json`

## 📌 Admin Panel

Open `index.html` in browser to manage users:

- Block / Unblock
- Set passwords
- Set `approved` status
- Edit max devices
